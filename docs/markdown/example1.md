# Example Document

## Introduction

This is a sample Markdown document used to test the `creation:create_pdf` skill.

## Features

- Simple formatting with **bold** and *italic* text
- Unordered and ordered lists
- Code blocks

## Steps to convert

1. Place your `.md` files in the source directory
2. Run the skill with the appropriate arguments
3. Retrieve the generated PDFs from the output directory

## Code example

```python
def hello(name: str) -> str:
    return f"Hello, {name}!"
```

## Conclusion

If this document appears correctly in PDF form, the conversion pipeline is working as expected.
